# Cursor / AI 约束（与本 PRD 对齐的最小子集）

- 使用 TypeScript；禁止在无类型处滥用 `any`。
- 解析与 FFT 等重计算放在 **Node runtime** 的 Route Handler 中；勿把整份大文件逻辑塞进 Client Component。
- 诊断中的数值与结构化特征须与 **日志可算结果** 一致；大模型仅基于服务端生成的「简报 + 已算 JSON」生成通俗解读与 SOP，不得捏造未在简报中出现的指标名或数值。
- 参数建议须引用 **官方文档** 或明确标注为「启发式草案」；本仓库 MVP 已内置 Betaflight 官方链接摘录供对话接口使用。
